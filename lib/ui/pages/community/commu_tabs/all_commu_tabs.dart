// lib/ui/pages/community_tabs/all_tab.dart

import 'package:flutter/material.dart';

class AllTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        '전체 탭의 콘텐츠',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
