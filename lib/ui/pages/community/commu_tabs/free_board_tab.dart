// lib/ui/pages/community_tabs/free_board_tab.dart

import 'package:flutter/material.dart';

class FreeBoardTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        '자유게시판 탭의 콘텐츠',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
