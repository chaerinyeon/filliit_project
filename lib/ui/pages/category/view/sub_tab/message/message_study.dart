// lib/ui/widgets/category_contents/call_history_content.dart

import 'package:flutter/material.dart';

class MessageStudy extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Message Study Content',
        style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
      ),
    );
  }
}
