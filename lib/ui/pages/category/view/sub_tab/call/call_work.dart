// lib/ui/widgets/category_contents/call_history_content.dart

import 'package:flutter/material.dart';

class CallWork extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Call Work',
        style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
      ),
    );
  }
}
