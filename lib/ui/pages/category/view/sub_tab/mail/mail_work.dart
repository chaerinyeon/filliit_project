// lib/ui/widgets/category_contents/mail_social_content.dart

import 'package:flutter/material.dart';

class MailWork extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Mail Work Content',
        style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
      ),
    );
  }
}
